  variantElement.querySelector("#deletevariant").addEventListener("click", e => {
    e.target.closest(".variant").remove();
  });

  target.append(variantElement);
};

const addQuestionField = () => {
  const quizElement = document.createElement("div");
  quizElement.className = "question";
  quizElement.innerHTML = `
    <div class="head">
        <button id="delete" title="Delete question">❌</button>